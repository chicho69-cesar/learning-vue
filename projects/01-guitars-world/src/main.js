import './style.css'

import { createApp } from 'vue'
import App from './App.vue'

/* Creamos nuestra aplicación de Vue utilizando el componente App,
y con la función mount para montarlo en el elemento con el id app
en el archivo index.html */
createApp(App).mount('#app')
